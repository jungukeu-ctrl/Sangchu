# PLAN.md — 상추훈련 프로젝트 작업 계획

> 이 파일은 프로젝트의 유일한 진실 공급원입니다.
> 작업 시작 전 반드시 읽고, 작업 완료 후 반드시 업데이트하세요.

---

## 현재 브랜치

| 항목 | 값 |
|---|---|
| 개발 브랜치 | `claude/review-sangchu-claude-docs-atK6W` |
| 기준 브랜치 | `main` |
| 배포 | GitHub Pages (`main` 기준 자동 배포) |

---

## 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 저장소 | `jungukeu-ctrl/Sangchu` |
| 스택 | Vanilla HTML/CSS/JS, 외부 의존성 없음 |
| 파일 | `index.html` (판단 가이드), `backtest.html` (백테스트 시뮬레이터) |
| 자본금 상수 | `CAP = 1,000,000` |
| 일 매수 한도 | `CAP × 4% = 40,000원` |
| 1회 단위 | `CAP × 2% = 20,000원` |

---

## 완료된 작업

| 날짜 | 작업 내용 | 파일 |
|---|---|---|
| 2026-04-19 | backtest.html 최초 구현 (캔버스 차트, 상추훈련 매매 시뮬레이터) | `backtest.html` |
| 2026-04-19 | 줌 슬라이더 추가 (20~600일, 현재 보기 구간 중심 유지) | `backtest.html` |
| 2026-04-19 | 거래 마커 추가 (B/S 아이콘, 캔들 저가 아래/고가 위) | `backtest.html` |
| 2026-04-19 | GitHub Pages 배포 구조 수립 | `index.html`, `backtest.html` |
| 2026-04-19 | 페이지 간 네비게이션 버튼 추가 | `index.html`, `backtest.html` |
| 2026-04-19 | PLAN.md 최초 작성 | `PLAN.md` |

---

## 남은 작업 목록

| 우선순위 | 작업 | 대상 파일 | 비고 |
|---|---|---|---|
| - | `naver_stock_fetcher.py` 저장소 포함 또는 README 안내 | `README.md` | 외부 스크립트 |
| - | 백테스트 결과 요약 패널 (총 거래수, 최대 낙폭, 승률) | `backtest.html` | |
| - | 모바일 터치 제스처 지원 (핀치 줌, 스와이프 스크롤) | `backtest.html` | 480px 미디어쿼리 |
| - | 여러 종목 동시 관리 (슬롯 개념 확장) | `backtest.html` | 구조 변경 큰 작업 |
| - | 판단 가이드에 중급(50%) / 고급(60~70%) 기준비중 모드 추가 | `index.html` | MAP 객체 확장 필요 |
| - | 로컬 스토리지로 거래 내역 세션 유지 | `backtest.html` | `st` 객체 직렬화 |

---

## 작업 규칙 요약

> 상세 규칙은 `CLAUDE.md` 참조

1. **`"코드작성"` 메시지 전까지 코드 수정 금지** — 분석/계획/설명만
2. 작업 완료 시 **`"코드작업완료"` 알림** 필수
3. 수정 전 영향도 분석 보고 → 확인(OK) 후 진행
4. 한 번에 하나만 수정, 요청 외 코드 건드리지 않음
5. 수정 후 변경 위치, 영향 기능, 수식 검증 보고

---

## 영향도 체크리스트 (수정 전 확인)

**backtest.html**
```
[ ] draw() — 레이어 순서 영향 여부
[ ] exec() — st 객체 변형 → render(), draw() 연쇄 영향
[ ] clickDay() / doNext() — 자동매도 로직 중복 여부
[ ] WIN / sOff 변경 — scrl.max 재계산 필요 여부
[ ] CAP 상수 — 모든 % 계산에 영향 (grep CAP 선행)
```

**index.html**
```
[ ] MAP 객체 — 36개 키 모두 커버되는지 확인
[ ] state — w/p/f 세 값 null 아닌 상태에서만 결과 출력
[ ] CSS 변수 — var(--xxx) 참조 누락 여부
[ ] 탭 전환 — panel.active / tab.active 클래스 동기화
```

**공통**
```
[ ] 페이지 간 링크 — 상대경로 유지
[ ] 색상 의미 — 빨강(매수)/파랑(매도) 혼용 금지
[ ] 모바일 뷰 — max-width 480px 미디어쿼리 동작 확인
```
